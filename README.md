# 🛒 E-commerce Backend API

Современный и масштабируемый API для интернет-магазина, построенный на базе **Django 6** и **DRF**. В проекте реализованы асинхронные возможности, обработка фоновых задач и полная контейнеризация для быстрого развертывания.

---

### Стек технологий

| Категория | Технология |
| :--- | :--- |
| **Framework** | Django 6 + DRF (Django Rest Framework) |
| **Database** | PostgreSQL 18 |
| **Authentication** | JWT (SimpleJWT) |
| **Task Queue** | Celery + RabbitMQ (broker) |
| **Web Server** | Nginx + Gunicorn (Uvicorn worker для ASGI) |
| **Documentation** | Swagger (drf-spectacular) |
| **Containerization** | Docker + Docker Compose |

---

### Основные возможности

* **JWT Авторизация:** Безопасная аутентификация с использованием `access` и `refresh` токенов.
* **Асинхронность:** Использование **ASGI** и асинхронных расширений DRF (`adrf`) для повышения производительности.
* **Фоновые задачи (Celery):**
    * Автоматический расчет средних рейтингов товаров.
    * **Django Signals:** Интеграция триггеров для запуска задач Celery при изменениях в БД.
* **Swagger UI:** Полная интерактивная документация всех эндпоинтов «из коробки».
* **Production-ready:** Конфигурация Nginx для отдачи статики/медиа и проксирования запросов.

---

### Производительность (Benchmarks)

> Проект протестирован в условиях, приближенных к реальной эксплуатации:
> * **Конфигурация:** 8 Gunicorn воркеров (`UvicornWorker`).
> * **Объем данных:** 100 000+ записей в базе данных PostgreSQL.
> * **Нагрузка:** 100 одновременных соединений в течение 30 секунд.
> * **Результат:** Стабильная пропускная способность **260 RPS** (Requests Per Second).

---

### Установка и запуск

Для запуска вам понадобятся **Docker** и **Docker Compose**.

#### 1. Настройка переменных окружения
Создайте файл `.env` в корневой директории на основе примера `.env.example`:

```env
# Django settings
DEBUG=1
SECRET_KEY=your_secret_key_here

# PostgreSQL settings
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=admin
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=app_db

# RabbitMQ settings
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASSWORD=guest
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
```

#### 2. Запуск через Docker Compose
Выполните команду для сборки и старта всех сервисов:

```bash
docker compose up -d --build
```
После запуска проект будет доступен по адресу: **http://127.0.0.1/**

---

### API Документация

Интерактивная документация доступна сразу после запуска:
* **Swagger UI:** [http://127.0.0.1/api/docs/](http://127.0.0.1/api/docs/)

---

### Архитектура контейнеров

* `backend`: Приложение Django (Gunicorn/Uvicorn).
* `postgres`: База данных PostgreSQL 18.
* `rabbitmq`: Брокер сообщений.
* `celery_worker`: Обработка фоновых задач.
* `nginx`: Обратный прокси-сервер.

---

### Основные команды

Используйте эти команды для управления проектом (миграции пробрасываются в Docker):

| Действие | Команда |
| :--- | :--- |
| **Создание миграций** | `./make_migrations.sh` |
| **Выполнение миграций** | `./migrate.sh` |
| **Логи воркера** | `docker compose logs -f celery_worker` |
| **Суперпользователь** | `docker compose exec backend python manage.py createsuperuser` |
| **Остановка** | `docker compose down` |

---
В данном проекте была реализована попытка выжать максимум производительности в DRF